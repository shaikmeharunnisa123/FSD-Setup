{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "py -3 -m app.main",
    "build": "py -3 -m pip install -r requirements.txt",
    "start": "py -3 -m gunicorn app.main:app --bind 0.0.0.0:5000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@railway/cli": "^4.4.1"
  }
}
